/* 更多按钮 */
.more {
  width: 60rpx;
  height: 60rpx;
  position: absolute;
  right: 10rpx;
  top: 50%;
  transform: translateY(-50%);
}
@media (min-width: 768px) {
  /* 更多按钮 */
  .more {
    width: 40rpx;
    height: 40rpx;
  }
  /* 设置字体大小 */
  view,
  text {
    font-size: 22rpx;
  }
}

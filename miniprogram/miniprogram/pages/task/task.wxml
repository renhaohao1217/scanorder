<!-- 导航栏 -->
<navigation-bar title="我的任务"></navigation-bar>
<!-- 我的任务内容区域 -->
<view class="task-container">
  <task-list wx:for="{{task_arr}}" wx:key="index" item="{{item}}" index="{{index}}" bindremove="remove" end="{{end}}" result="{{end[index]}}" bindupdate="update"></task-list>
</view>